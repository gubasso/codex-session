# Cross-Repo Verification — 2026-06-02

Checklist re-derived from the three authoritative plan files in `.plan/configs-rename-split-profiles/` and `.plan/prex-sandbox-fix-tmpdir-migration/`; it was not copied from the round spec. No additional concrete cross-repo claims beyond C1-C10 required separate verification.

Audited repos and resolved paths:

- `~/.dotfiles/codex-session/.config/codex-session/`
- `~/.claude/skills/prex/SKILL.md`
- `~/DocsNNotes/tech/tools/claude-code/codex-conventions.md`
- `~/DocsNNotes/tech/tools/claude-code/AGENTS.md`
- `~/.dotfiles/dctl/.config/dctl/devcontainer/`

Resolved paths differ from the round spec's literal paths: the codex-session tree lives under `~/.dotfiles/codex-session/.config/codex-session/`, the conventions docs live under `~/DocsNNotes/tech/tools/claude-code/`, and there is no separate `~/.claude/skills/prex-resume/` skill.

## configs-rename-split-profiles

| Claim                                                                                                                      | Verdict   | Evidence                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1. dotfiles codex-session tree uses sibling `configs/` + `profiles/`; no `settings/` or `settings.bak.*`.                 | confirmed | `ls -la "$DCS" "$DCS/configs" "$DCS/profiles"` showed `config.toml`, `config-recipes/`, `configs/{base,plugins,projects}.toml`, and sibling `profiles/`; `ls -ld "$DCS/settings" "$DCS"/settings.bak.* 2>&1` returned `No such file or directory` for both absent legacy paths.                     |
| C2. `config-recipes/default.yaml` uses `config-layers:`, not `settings-layers:`.                                           | confirmed | `rg -n 'config-layers\|settings-layers' "$DCS/config-recipes/default.yaml"` returned `1:config-layers:` only.                                                                                                                                                                                       |
| C3. `config.toml` references sibling `profiles/`, not `configs/profiles/`.                                                 | confirmed | `rg -n 'profiles' "$DCS/config.toml"` returned `3:# overrides under sibling profiles/<name>.config.toml).`                                                                                                                                                                                          |
| C4. No residual `settings-layers`, `configs/profiles`, or `[profiles.*]` tables remain in the dotfiles codex-session tree. | confirmed | `rg -n 'config-layers\|settings-layers\|configs/profiles\|\\[profiles\\.\|profiles/' "$DCS"` returned only current-layout hits in `config.toml:3`, `configs/base.toml:2`, and `config-recipes/default.yaml:1`; no `settings-layers`, `configs/profiles`, or `[profiles.*]` matches.                 |
| C5. `codex-conventions.md` references the sibling `profiles/` layout with no live legacy-shape guidance.                   | confirmed | `rg -n 'profiles/\|configs/profiles\|settings-layers\|config-layers\|\\[profiles\\.' "$CONV"` returned `14-15` and `112-113`, all describing the current sibling layout and explicitly saying profile files have `no [profiles.<name>] header`; no `configs/profiles` or `settings-layers` matches. |

## prex-sandbox-fix-tmpdir-migration

| Claim                                                                                                                                                                      | Verdict   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C6. Unified bypass pattern applies to both stage-1 exec and the in-skill resume path; no resume sandbox mismatch.                                                          | diverged  | `find ~/.claude/skills -maxdepth 2 -name SKILL.md -path '*prex*'` found only `~/.claude/skills/prex/SKILL.md`, and `find ~/.claude/skills -iname '*resume*'` returned no matches, so the plan's separate `prex-resume` skill topology is inaccurate. The underlying behavior is present in `prex/SKILL.md`: `rg -n -- '--dangerously-bypass-approvals-and-sandbox\|resume' "$PREX"` returned stage-1 at `351`, resume call at `501-502`, and fallback fresh exec at `545`.                                                                                                                    |
| C7. Migrated skill run directories use `${XDG_STATE_HOME:-$HOME/.local/state}/claude-session/skill-runs/`; no hardcoded `/tmp` run dirs remain across the migrated skills. | confirmed | `rg -n 'mktemp -d /tmp\|RUN_DIR="\\$\\(mktemp -d /tmp\|STAGE_DIR="\\$\\(mktemp -d /tmp\|LOCK_DIR="\\$\\{XDG_RUNTIME_DIR:-/tmp\\}' ~/.claude/skills` returned no matches. Positive checks from `rg -n -- 'skill-runs\|XDG_STATE_HOME\|/tmp' ~/.claude/skills` show XDG-rooted run dirs in `prex/SKILL.md:137,172,728,757`, `review-loop/SKILL.md:93`, `tsk-new/SKILL.md:234`, `ask/SKILL.md:104`, `tsk-impl/SKILL.md:60`, and `refactor-migration-plan/SKILL.md:114`; the remaining `/tmp` hits are documentation warnings in `skill-builder/SKILL.md:64,165`, not active run-dir definitions. |
| C8. `codex-conventions.md` carries Resume Constraint, `--full-auto` deprecation, Skill Run Directories, and the safety matrix.                                             | confirmed | `rg -ni 'safety matrix\|resume constraint\|skill run directories\|full-auto' "$CONV"` returned `Resume Constraint` at `358`, `Skill Run Directories` at `462`, and `--full-auto` references at `181,217,308,368,499`. `sed -n '458,508p' "$CONV"` shows the unified sandbox flag matrix under `## Safety Rules`; the heading is `Safety Rules`, not a literal `safety matrix` heading.                                                                                                                                                                                                        |
| C9. `AGENTS.md` `last-synced` matches the source-plan generation date `2026-05-28`.                                                                                        | confirmed | `rg -n 'last-synced\|last_synced' ~/DocsNNotes/tech/tools/claude-code/AGENTS.md` returned `3:last-synced: 2026-05-28`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C10. No dctl `devcontainer.json` bind-mounts `/tmp`.                                                                                                                       | confirmed | `rg -n 'source\": \"/tmp\"\|target\": \"/tmp\"\|source=/tmp\|target=/tmp' ~/.dotfiles/dctl` returned no matches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Follow-ups

- `diverged` in `~/.claude/skills` / source plan docs: the two prex-sandbox plan files still frame resume as a separate `prex-resume` skill, but the live topology is a single `prex/SKILL.md` that contains both the stage-1 exec path and the stage-3 resume path. This warranted a documentation correction in the source plan and, if any companion docs still repeated it, the skill documentation repo.
  - **Resolved (in-repo, 2026-06-02):** a divergence note was appended to `.plan/prex-sandbox-fix-tmpdir-migration/_README.md` under its "Implementation Notes / Divergences" section, recording that `prex-resume` is not a separate skill (resume is unified inside `prex/SKILL.md`). Per the cleanup plan's house rule the historical round text is left intact as the record.
  - **No external fix needed:** the live skill/conventions docs are already clean — `grep -rn 'prex-resume' ~/.claude/skills ~/DocsNNotes/tech/tools/claude-code` returns no matches, so no companion-doc correction is outstanding in the external repos.

## Confidence Note

All three external repos were accessible and readable in this environment on 2026-06-02. There were no `not-accessible` items.
